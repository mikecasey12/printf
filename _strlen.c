#include "holberton.h"

/**
 * _strlen - a function that gets the length of string
 * @s: string to get length
 *
 * Return: len
 */
int _strlen(char *s)
{
	int len;

	for (len = 0; s[len] != 0; len++)
	{
	}
	return (len);
}
